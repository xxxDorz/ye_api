import * as React from 'react';
import './styles.css';
export interface InspectProps {
    children: React.ReactElement | React.ReactElement[];
    margin?: boolean;
    padding?: boolean;
    size?: boolean;
    disabled?: boolean;
}
export default function Inspect({ children, margin, padding, size, disabled, }: InspectProps): JSX.Element;
